# Data Layout

- `raw/`: connector payloads and source snapshots
- `raw/http_cache/`: local HTTP cache for repeated GET requests
- `processed/`: normalized datasets
- `features/`: engineered features and scores

These directories are intended for local artifacts and should generally stay out of version control.
